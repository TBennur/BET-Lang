fun fact (n::int)::int {
  let (
    i:=(1),
    acc:=(1)
  ) { 
    do { 
      if ((i) < (n)) { 
        i := ((i) + (1));
        acc := ((acc) * (i)) 
      } else { 
        acc 
      } 
    } until ( 
      (i) >= (n) 
    ) 
  }
};

fact(input)