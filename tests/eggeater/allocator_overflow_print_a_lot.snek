(struct ll ((val int) (next ll)))

(let 
    (
        (x (alloc ll)) 
        (y 1)
    ) 
	(repeat-until  
		    (block
                (set! x (alloc ll)) 
                (set! y (add1 y))
                (print y)
            )
            false
    )
)

